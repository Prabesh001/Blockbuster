import { View, Text } from "react-native";
import React from "react";

const Saved = () => {
  return (
    <View className="bg-primary flex-1">
      <Text className="flex-1 bg-primary text-white">Saved</Text>
    </View>
  );
};

export default Saved;
